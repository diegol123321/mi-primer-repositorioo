// Función para transmitir la velocidad en un cluster
#include <stdio.h>

void transmitir_velocidad_cluster(double velocidad) {
   // Simulación de transmisión: imprimir el valor
   printf("Transmisión de velocidad al cluster: %.2f km/h\n", velocidad);
   // Aquí se podría agregar el código para enviar el dato por CAN, UART, etc.
}